services:
  php:
    build:
      context: .
      args:
        PHP_VERSION: "8.4"
    volumes:
      - .:/app
      - composer-cache:/root/.composer/cache
    working_dir: /app

volumes:
  composer-cache:
